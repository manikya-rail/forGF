<div class="row" style="margin-top:10px;">
	<% hole.hole_image.each do | holeimage | %>
		<div class="col-md-3">
	    <div class="thumbnail">
	      <a href="<%= holeimage.image.url %>">
	        <%=image_tag holeimage.image.url(:thumb) %>
	      </a>
	    </div>
	  </div>
	<% end%>
</div>