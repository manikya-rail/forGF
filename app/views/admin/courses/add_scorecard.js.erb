var last_scorecard = $(".scorecard").last();
$(last_scorecard).after("<%= j render(partial: 'scorecard_form', locals: {scorecard_index: @scorecard_index}) %>");
if ($(".scorecard").length > 1){
  $("#remove_scorecard").show();
}